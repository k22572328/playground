# 編譯產物。不限定在根目錄，因為執行檔常被編在 cmd/server/ 底下。
# 這裡只列實際會產生的檔名，避免 "server" 這種通用字誤擋到原始碼目錄。
bigtwo
core
*.exe

# 測試與分析
*.test
*.out
coverage.html

# 編輯器與系統
.DS_Store
.idea/
.vscode/

# 牌局紀錄（任何目錄底下的 logs/）
logs/

# 本機的 MCP 設定（Playwright 用 headless 跑，不跳出瀏覽器視窗）
.mcp.json

# Playwright 的暫存輸出
.playwright-mcp/

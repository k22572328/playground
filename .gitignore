# 編譯產物。一律加上開頭的斜線綁在根目錄，否則 git 會把規則套到每一層，
# 連 internal/games/bigtwo/ 與 web/games/bigtwo/ 這種原始碼目錄都會被吃掉。
# cmd/server/ 底下的執行檔另外列出來。
/bigtwo
/cmd/server/bigtwo
/core
/cmd/server/core
*.exe

# 測試與分析
*.test
*.out
coverage.html

# 編輯器與系統
.DS_Store
.idea/
.vscode/

# 牌局紀錄（任何目錄底下的 logs/）
logs/

# 本機的 MCP 設定（Playwright 用 headless 跑，不跳出瀏覽器視窗）
.mcp.json

# Playwright 的暫存輸出
.playwright-mcp/
